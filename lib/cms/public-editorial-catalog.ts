import{listEditorialPosts,findEditorialPostBySlug}from"./editorial-catalog";import{getPostService}from"./cloudflare-context";import{editorial}from"./post-service";export async function listPublicEditorialPosts(){const legacy=listEditorialPosts();try{const remote=(await getPostService().published()).map(editorial),seen=new Set<string>();return[...remote,...legacy].filter(p=>!seen.has(p.slug)&&seen.add(p.slug)).sort((a,b)=>b.date.localeCompare(a.date)||a.slug.localeCompare(b.slug))}catch(e){console.error(JSON.stringify({scope:"cms.public",event:"d1.unavailable",message:e instanceof Error?e.message:"unknown"}));return legacy}}export async function findPublicEditorialPostBySlug(slug:string){try{const p=await getPostService().publishedBySlug(slug);if(p)return editorial(p)}catch{}return findEditorialPostBySlug(slug)}
