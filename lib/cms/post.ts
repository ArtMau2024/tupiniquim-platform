import {BLOG_CATEGORIES} from "../blog-categories";
export type PostStatus="draft"|"published";export type PostInput={title:string;slug:string;description:string;category:string;content:string;author?:string|null;image?:string|null};export type Post=Required<Omit<PostInput,"image">>&{id:string;image:string|null;status:PostStatus;createdAt:string;updatedAt:string;publishedAt:string|null};export type PostErrors=Partial<Record<keyof PostInput,string>>;const slug=/^[a-z0-9]+(?:-[a-z0-9]+)*$/;const categories=new Set<string>(BLOG_CATEGORIES.map(x=>x.slug));const clean=(x:unknown)=>typeof x==="string"?x.trim():"";export function validatePostInput(raw:Record<string,unknown>){const value={title:clean(raw.title),slug:clean(raw.slug).toLowerCase(),description:clean(raw.description),category:clean(raw.category),content:clean(raw.content),author:clean(raw.author)||"Greyce",image:clean(raw.image)||null};const errors:PostErrors={};if(!value.title)errors.title="Informe o título.";if(!slug.test(value.slug))errors.slug="Slug inválido.";if(!value.description)errors.description="Informe a descrição.";if(!categories.has(value.category))errors.category="Categoria inválida.";if(!value.content)errors.content="Informe o conteúdo.";return Object.keys(errors).length?{ok:false as const,errors}:{ok:true as const,value}}export function mapPost(x:any):Post{const p={id:String(x.id),slug:String(x.slug),title:String(x.title),description:String(x.description),category:String(x.category),content:String(x.content),author:String(x.author||"Greyce"),image:x.image==null?null:String(x.image),status:x.status as PostStatus,createdAt:String(x.created_at),updatedAt:String(x.updated_at),publishedAt:x.published_at==null?null:String(x.published_at)};if(p.status==="draft"&&p.publishedAt!==null)throw new Error("Estado editorial inválido.");if(p.status==="published"&&!p.publishedAt)throw new Error("Estado editorial inválido.");return p}
