import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const read=p=>fs.readFileSync(new URL(`../../../${p}`,import.meta.url),"utf8");test("páginas e ações protegem sessão e não publicam",()=>{for(const p of ["app/admin/posts/novo/page.tsx","app/admin/rascunhos/[id]/editar/page.tsx","app/admin/posts/draft-actions.ts"]){const s=read(p);assert.match(s,/hasValidAdminSession/);assert.match(s,/\/admin\/login/);assert.doesNotMatch(s,/deploy|opennext|next build|git push/i);}});test("listagem separa rascunhos e publicados",()=>{const s=read("app/admin/posts/page.tsx");assert.match(s,/Rascunhos/);assert.match(s,/Artigos publicados/);});test("ações locais são separadas e declaram limites",()=>{const p=read("app/admin/rascunhos/[id]/editar/page.tsx"),a=read("app/admin/posts/draft-actions.ts");assert.match(p,/Gerar artigo local/);assert.match(p,/Gerar catálogo local/);assert.match(p,/Aguardando validação local/);assert.doesNotMatch(a,/child_process|generate-posts/);});
