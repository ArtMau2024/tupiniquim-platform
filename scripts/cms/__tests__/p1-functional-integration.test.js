import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const read=p=>fs.readFileSync(p,"utf8");test("actions usam PostService e protegem sessao",()=>{const s=read("app/admin/posts/post-actions.ts");assert.match(s,/hasValidAdminSession/);assert.match(s,/getPostService/);assert.doesNotMatch(s,/D1PostRepository|CMS_DB|\.prepare\(/)});test("painel separa ciclo CMS e legado",()=>{const s=read("app/admin/posts/page.tsx");for(const text of["Rascunhos","Publicados pelo CMS","MDX legados"])assert.match(s,new RegExp(text));assert.match(s,/\/admin\/posts\/\$\{post\.id\}\/editar/);assert.doesNotMatch(s,/Gerar artigo local|Gerar catalogo local/)});test("edicao oferece acao conforme estado",()=>{const s=read("app/admin/posts/[id]/editar/page.tsx");assert.match(s,/Publicar/);assert.match(s,/Retirar do ar/);assert.match(s,/slugLocked/)});test("rotas publicas usam catalogo hibrido",()=>{for(const p of["app/blog/page.tsx","app/blog/categoria/[categoria]/page.tsx","app/blog/[slug]/page.tsx"]){const s=read(p);assert.match(s,/public-editorial-catalog/);assert.doesNotMatch(s,/CMS_DB|D1Database|D1PostRepository|\.prepare\(/)}});test("experiencia principal nao usa geradores locais",()=>{for(const p of["app/admin/posts/novo/page.tsx","app/admin/posts/page.tsx","app/admin/posts/[id]/editar/page.tsx"]){const s=read(p);assert.doesNotMatch(s,/generateLocalMdx|generateLocalCatalog|Gerar artigo local|Gerar catalogo local/)}});
