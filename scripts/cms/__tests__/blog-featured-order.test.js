import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const page=fs.readFileSync("app/blog/page.tsx","utf8"),catalog=fs.readFileSync("lib/cms/public-editorial-catalog.ts","utf8");test("pagina usa catalogo publico hibrido",()=>{assert.match(page,/listPublicEditorialPosts/);assert.match(page,/featuredPost = posts\[0\]/);assert.match(page,/secondaryPosts = posts\.slice\(1, 3\)/)});test("catalogo ordena e resolve colisao",()=>{assert.match(catalog,/seen/);assert.match(catalog,/sort/);assert.match(catalog,/published/);assert.match(catalog,/return legacy/)})
