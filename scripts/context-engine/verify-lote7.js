"use strict";
const fs=require("fs");const path=require("path");const {classifyIntent}=require("./intent-router");const {buildContextPackage}=require("./context-package");const {fallback,validateAnswer}=require("./contextual-answer");const ROOT=process.cwd();try{const map=JSON.parse(fs.readFileSync(path.join(ROOT,"site-context","project-map.json"),"utf8"));const index=JSON.parse(fs.readFileSync(path.join(ROOT,"site-context","context-index.json"),"utf8"));const cases=[["Qual o proximo passo do projeto?","product_roadmap"],["Qual a proxima evolucao do Context Engine?","context_engine_roadmap"],["Quais riscos tecnicos estao abertos?","technical_risk"],["Quais decisoes arquiteturais estao ativas?","architecture_decision"],["Onde esta a logica das categorias?","code_search"]];for(const [q,expected] of cases){const intent=classifyIntent(q);if(intent!==expected)throw new Error(`${q} => ${intent}, expected ${expected}`);const pkg=buildContextPackage({question:q,intent,map,index});validateAnswer(fallback(pkg),pkg);}console.log("Context Engine Lote 7 deterministic routing and fallback verified successfully.");}catch(e){console.error(e.message);process.exit(1);}
